# Examples

In time, this directory will contain Jupyter notebooks that demonstrate how to access Pandects data and that show off some of the interesting analyses that the Pandects dataset enables. For now, we feature just a simple quickstart notebook.

View complete API documentation on the [Docs](https://pandects.org/docs) page.

- `pandects_api_quickstart.ipynb`: quickstart API walkthrough (auth + downloads).
