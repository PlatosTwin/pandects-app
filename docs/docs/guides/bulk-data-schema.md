---
id: bulk-data-schema
title: Bulk Data Schema
description: Table-by-table reference for the Pandects public database dump.
sidebar_position: 4
---

{/* GENERATED FILE — do not edit by hand.
    Built by bulk/schema_docs/generate_schema_docs.py from the live schema
    plus bulk/schema_docs/table_docs.yml. Regenerated by bulk/push_to_r2.sh. */}

# Bulk Data Schema

Pandects publishes its full public dataset as a MariaDB dump on Cloudflare R2.
This page documents every table in that dump.

## Getting the dump

- **Dump**: [https://bulk.pandects.org/dumps/latest.sql.gz](https://bulk.pandects.org/dumps/latest.sql.gz)
- **Checksum**: [https://bulk.pandects.org/dumps/latest.sql.gz.sha256](https://bulk.pandects.org/dumps/latest.sql.gz.sha256)
- **Manifest** (size, SHA-256, timestamp): [https://bulk.pandects.org/dumps/latest.json](https://bulk.pandects.org/dumps/latest.json)

Restore into a local MariaDB database:

```bash
curl -LO https://bulk.pandects.org/dumps/latest.sql.gz
gunzip latest.sql.gz
mysql -e "CREATE DATABASE pandects"
mysql pandects < latest.sql
```

The dump targets MariaDB 11+. `sections.embedding` uses the MariaDB
`VECTOR` type; on servers without vector support, load the dump with that
column's DDL adjusted or skip the `sections` table.

An interactive ER diagram of this schema is published at [dbdocs.io](https://dbdocs.io/nmbogdan/Pandects).

## Conventions

- UUIDs are stored as `char(36)` strings.
- Boolean flags are `tinyint(1)` with values 0/1.
- Timestamps are UTC.
- Money columns are USD.

## Agreements and documents

The corpus itself: one row per merger agreement, plus the versioned XML conversions, the per-section breakdown of the latest XML, and the clause-level spans carved out of sections.

### `agreements`

One row per merger agreement. Rows are discovered from SEC EDGAR filings (or the original DMA seed corpus) and enriched with AI-extracted deal metadata. This is the root table: nearly every other table references `agreement_uuid`.

**Primary key**: `agreement_uuid`

**Relationships**:
- `agreement_counsel.agreement_uuid` → `agreements.agreement_uuid` (one-to-many)
- `sections.agreement_uuid` → `agreements.agreement_uuid` (one-to-many)
- `clauses.agreement_uuid` → `agreements.agreement_uuid` (one-to-many)
- `xml.agreement_uuid` → `agreements.agreement_uuid` (one-to-many)
- `latest_sections_search.agreement_uuid` → `agreements.agreement_uuid` (one-to-many)
- `agreement_index_summary.agreement_uuid` ↔ `agreements.agreement_uuid` (one-to-one)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `agreement_uuid` | `char(36)` | no | Primary key (UUID). |
| `url` | `varchar(512)` | no | EDGAR URL of the filing exhibit the agreement was ingested from. |
| `secondary_filing_url` | `varchar(512)` | yes | URL of a duplicate filing of the same agreement, when one was detected during ingestion. |
| `target` | `varchar(2048)` | yes | Name of the target company. |
| `acquirer` | `varchar(2048)` | yes | Name of the acquiring company. |
| `filing_date` | `date` | yes | Date the agreement was filed with the SEC. |
| `prob_filing` | `float` | yes | Classifier probability (0-1) that the source filing actually contains a merger agreement. |
| `filing_company_name` | `varchar(1024)` | yes | Name of the EDGAR filer that submitted the filing. |
| `filing_company_cik` | `varchar(256)` | yes | SEC Central Index Key (CIK) of the filer. |
| `form_type` | `varchar(256)` | yes | SEC form type of the parent filing (e.g. 8-K, S-4, DEFM14A). |
| `exhibit_type` | `enum('10','2','99')` | yes | Exhibit series the agreement appeared as: 2 (EX-2.x), 10 (EX-10.x), or 99 (EX-99.x). |
| `transaction_price_total` | `decimal(18,2)` | yes | Total transaction value, in USD. |
| `transaction_price_stock` | `decimal(18,2)` | yes | Stock component of the transaction value, in USD. |
| `transaction_price_cash` | `decimal(18,2)` | yes | Cash component of the transaction value, in USD. |
| `transaction_price_assets` | `decimal(18,2)` | yes | Asset component of the transaction value, in USD. |
| `transaction_consideration` | `enum('stock','cash','assets','mixed','unknown')` | yes | Form of consideration paid to target holders. |
| `target_type` | `enum('public','private')` | yes | Whether the target is a public or private company. |
| `acquirer_type` | `enum('public','private')` | yes | Whether the acquirer is a public or private company. |
| `target_industry` | `varchar(128)` | yes | Industry label for the target. |
| `acquirer_industry` | `varchar(128)` | yes | Industry label for the acquirer. |
| `announce_date` | `date` | yes | Date the deal was publicly announced. |
| `close_date` | `date` | yes | Date the deal closed, if it completed. |
| `deal_status` | `enum('pending','complete','cancelled')` | yes | Deal outcome to date. |
| `attitude` | `enum('friendly','hostile','unsolicited')` | yes | Deal attitude (friendly, hostile, or unsolicited). |
| `deal_type` | `enum('merger','stock_acquisition','asset_acquisition','tender_offer','membership_interest_purchase')` | yes | Transaction structure. |
| `purpose` | `enum('strategic','financial')` | yes | Whether the acquisition is strategic or financial. |
| `target_pe` | `tinyint(1)` | yes | 1 if the target is private-equity-owned or -backed. |
| `acquirer_pe` | `tinyint(1)` | yes | 1 if the acquirer is private-equity-owned or -backed. |
| `metadata_sources` | `mediumtext` | yes | JSON blob of per-field citations backing the extracted deal metadata (field, source URL, supporting quote). |
| `metadata` | `tinyint(1)` | yes | 1 once AI deal-metadata extraction has completed for this agreement. |
| `verified` | `tinyint(1)` | yes | 1 if the agreement has passed validation. |
| `seed` | `tinyint(1)` | yes | Internal flag marking rows from the original seed corpus. |
| `status` | `enum('verified','invalid')` | yes | Validation status. verified means the document is a well-formed merger agreement; invalid means it was rejected during validation. |
| `paginated` | `tinyint(1)` | yes | 1 once the source document has been split into pages by the pre-processing pipeline (the page-level tables are internal and not part of the public dump). |
| `source` | `enum('dma','edgar')` | yes | Where the record originated: dma (original seed corpus) or edgar (ongoing SEC EDGAR discovery). |
| `ingested_date` | `datetime` | yes | When the row was first ingested (UTC). |
| `updated_date` | `datetime` | yes | When the row was last updated (UTC). |
| `gated` | `tinyint(1)` | yes | 1 holds the agreement out of public app/API surfaces until it is verified. Gated, unverified agreements are excluded from API results. |
| `validation_priority` | `double` | yes | Score used to order agreements in the internal validation queue. |
| `metadata_uncited_fields` | `longtext` | yes | JSON list of extracted metadata fields that lack a supporting citation. |
| `target_counsel` | `longtext` | yes | JSON array of law-firm names advising the target, as extracted from the agreement. Normalized form lives in `counsel` / `agreement_counsel`. |
| `acquirer_counsel` | `longtext` | yes | JSON array of law-firm names advising the acquirer, as extracted from the agreement. Normalized form lives in `counsel` / `agreement_counsel`. |
| `utils_abandoned` | `tinyint(1)` | no | Internal flag hiding the agreement from the maintainer review UI. No public meaning. |

**Indexes**: `idx_agreements_acquirer` (`acquirer`); `idx_agreements_date` (`filing_date`); `idx_agreements_deal_type_filing_uuid` (`deal_type`, `filing_date`, `agreement_uuid`); `idx_agreements_metadata_filing` (`metadata`, `filing_date`, `agreement_uuid`); `idx_agreements_status_prob` (`status`, `prob_filing`, `filing_date`); `idx_agreements_target` (`target`); `idx_agreements_target_industry_filing_uuid` (`target_industry`, `filing_date`, `agreement_uuid`); `idx_agreements_validation_priority_desc_uuid_asc` (`validation_priority`, `agreement_uuid`); `idx_agreements_validation_priority_uuid` (`validation_priority`, `agreement_uuid`)

### `xml`

Versioned XML conversions of each agreement. The ETL pipeline converts the raw filing into structured XML (articles, sections, sub-clauses); re-runs produce new versions rather than overwriting. Use `latest = 1` and `status = 'verified'` for the current public text of an agreement.

**Primary key**: `agreement_uuid`, `version`

**Relationships**:
- `xml.agreement_uuid` → `agreements.agreement_uuid` (many-to-one)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `agreement_uuid` | `char(36)` | no | Agreement this XML conversion belongs to (composite PK with `version`). |
| `xml` | `longtext` | no | Full agreement text as structured Pandects XML. |
| `version` | `int(11)` | no | Version number of the conversion for this agreement (composite PK with `agreement_uuid`). |
| `status` | `enum('verified','invalid')` | yes | Validation status of this XML version. |
| `status_source` | `enum('asset','manual')` | yes | How the status was assigned: asset (automated pipeline validation) or manual (human review). |
| `created_date` | `datetime(3)` | yes | When this version was produced (UTC). |
| `latest` | `tinyint(1)` | no | 1 for the newest XML version of each agreement; older versions keep 0. |
| `gated` | `tinyint(1)` | yes | 1 if this XML version is gated out of public serving. |
| `validation_priority` | `tinyint(1)` | yes | Internal review-queue priority flag. |
| `status_reason_code` | `varchar(64)` | yes | Machine-readable reason an XML version was marked invalid (e.g. section_article_mismatch, too_few_articles, llm_invalid). |
| `status_reason_detail` | `text` | yes | Free-text detail accompanying `status_reason_code`. |
| `ai_repair_attempted` | `tinyint(1)` | no | 1 if the AI-repair stage attempted to fix this XML version. |

**Indexes**: `idx_xml_gated` (`gated`); `idx_xml_latest` (`agreement_uuid`, `latest`); `idx_xml_latest_status_gated_agreement` (`latest`, `status`, `gated`, `agreement_uuid`); `idx_xml_status_agreement` (`status`, `agreement_uuid`)

### `sections`

One row per section of each agreement's XML, in document order. This is the main unit of retrieval in the app and API: sections carry the section text, taxonomy labels, and a vector embedding.

**Primary key**: `section_uuid`

**Relationships**:
- `sections.agreement_uuid` → `agreements.agreement_uuid` (many-to-one)
- `clauses.section_uuid` → `sections.section_uuid` (one-to-many)
- `latest_sections_search.section_uuid` ↔ `sections.section_uuid` (one-to-one)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `section_uuid` | `char(36)` | no | Primary key (UUID). |
| `agreement_uuid` | `char(36)` | no | Agreement the section belongs to. |
| `article_title` | `mediumtext` | yes | Title of the article (top-level heading) containing this section, as written in the agreement. |
| `article_title_normed` | `mediumtext` | yes | Normalized article title used for matching. |
| `article_order` | `int(11)` | yes | Position of the article within the agreement. |
| `section_title` | `mediumtext` | yes | Title of the section, as written in the agreement. |
| `section_title_normed` | `mediumtext` | yes | Normalized section title used for matching. |
| `section_order` | `int(11)` | yes | Position of the section within its article. |
| `xml_content` | `longtext` | no | XML markup of the section body. |
| `section_standard_id` | `longtext` | yes | Legacy single-value section-taxonomy assignment. Currently unpopulated; use `section_standard_id_gold_label`. |
| `section_standard_id_gold_label` | `longtext` | yes | JSON array of section-taxonomy `standard_id`s (nodes of `taxonomy_l2` / `taxonomy_l3`) assigned to this section. |
| `xml_version` | `int(11)` | yes | The `xml.version` this section was extracted from. |
| `embedding` | `vector(1024)` | yes | 1024-dimension MariaDB VECTOR embedding of the section text, used for semantic search. |
| `gold_label_model` | `enum('gpt-5.1','gpt-5-mini','gpt-5.4','gpt-5.4-mini','deepseek-v4-pro')` | yes | Model that produced `section_standard_id_gold_label`. |

**Indexes**: `idx_sections_agreement` (`agreement_uuid`); `idx_sections_agreement_stdid` (`agreement_uuid`, `section_standard_id`); `idx_sections_agreement_version` (`agreement_uuid`, `xml_version`); `idx_sections_article_normed` (`article_title_normed`); `idx_sections_article_order` (`agreement_uuid`, `article_order`); `idx_sections_section_normed` (`section_title_normed`); `idx_sections_section_order` (`agreement_uuid`, `article_order`, `section_order`); `idx_sections_standard_id_gold_label_prefix` (`section_standard_id_gold_label`); `idx_sections_standard_id_prefix` (`section_standard_id`); `idx_sections_stdid_null` (`section_standard_id`, `agreement_uuid`); `idx_sections_xml_version` (`xml_version`)

### `clauses`

Clause-level spans carved out of sections for clause-taxonomy modules. Each row is a contiguous span of a section's text. Only sections relevant to a module are carved; currently the only module is `tax`.

**Primary key**: `clause_uuid`

**Relationships**:
- `clauses.agreement_uuid` → `agreements.agreement_uuid` (many-to-one)
- `clauses.section_uuid` → `sections.section_uuid` (many-to-one)
- `tax_clause_assignments.clause_uuid` → `clauses.clause_uuid` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `clause_uuid` | `char(36)` | no | Primary key (UUID). |
| `agreement_uuid` | `char(36)` | no | Agreement the clause belongs to. |
| `section_uuid` | `char(36)` | no | Section the clause was carved from. |
| `xml_version` | `int(11)` | yes | XML version of the parent section at extraction time. |
| `module` | `varchar(64)` | no | Clause-taxonomy module the clause was extracted for. Currently always tax. |
| `clause_order` | `int(11)` | no | Order of the clause within its section. |
| `anchor_label` | `varchar(64)` | yes | Subsection anchor the clause starts at (e.g. (a), (h)), when present. |
| `start_char` | `int(11)` | no | Start character offset of the clause span within the section text. |
| `end_char` | `int(11)` | no | End character offset (exclusive) of the clause span within the section text. |
| `clause_text` | `longtext` | no | Plain text of the clause span. |
| `source_method` | `varchar(64)` | no | How the span was carved: paragraph_split, enumerated_split, section_title_match, or whole_section_fallback. |
| `context_type` | `varchar(64)` | no | Where the clause sits in the agreement: operative provisions or rep_warranty (representations and warranties). |
| `created_at` | `datetime` | no | Row creation time (UTC). |
| `updated_at` | `datetime` | no | Row last-update time (UTC). |

**Indexes**: `idx_clauses_agreement_module` (`agreement_uuid`, `module`); `idx_clauses_module_agreement_xmlver` (`module`, `agreement_uuid`, `xml_version`); `idx_clauses_module_context_agreement_order` (`module`, `context_type`, `agreement_uuid`, `section_uuid`, `clause_order`, `clause_uuid`); `idx_clauses_module_xml_version` (`module`, `xml_version`); `idx_clauses_section_module` (`section_uuid`, `module`)

## Section taxonomy

A three-level taxonomy of merger-agreement section types. Sections are labeled with these `standard_id`s via `sections.section_standard_id_gold_label` and, in exploded form, via `latest_sections_search_standard_ids`.

### `taxonomy_l1`

Top level of the section taxonomy — broad categories such as "Definitions and Interpretation" or "Representations and Warranties (Company/Seller)".

**Primary key**: `standard_id`

**Relationships**:
- `taxonomy_l2.parent_id` → `taxonomy_l1.standard_id` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `standard_id` | `char(16)` | no | Primary key; 16-character hex identifier. |
| `label` | `char(128)` | yes | Human-readable category name. |
| `parent_id` | `char(16)` | yes | Always NULL at the top level. |

### `taxonomy_l2`

Middle level of the section taxonomy.

**Primary key**: `standard_id`

**Relationships**:
- `taxonomy_l2.parent_id` → `taxonomy_l1.standard_id` (many-to-one)
- `taxonomy_l3.parent_id` → `taxonomy_l2.standard_id` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `standard_id` | `char(16)` | no | Primary key; 16-character hex identifier. |
| `label` | `char(128)` | yes | Human-readable category name. |
| `parent_id` | `char(16)` | yes | The `taxonomy_l1.standard_id` this node belongs to. |

### `taxonomy_l3`

Leaf level of the section taxonomy — the most specific section types (e.g. "Termination Events; Outside Date; Extensions").

**Primary key**: `standard_id`

**Relationships**:
- `taxonomy_l3.parent_id` → `taxonomy_l2.standard_id` (many-to-one)
- `latest_sections_search_standard_ids.standard_id` → `taxonomy_l3.standard_id` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `standard_id` | `char(16)` | no | Primary key; 16-character hex identifier. |
| `label` | `char(128)` | yes | Human-readable category name. |
| `parent_id` | `char(16)` | yes | The `taxonomy_l2.standard_id` this node belongs to. |

## Clause taxonomy (tax module)

A separate, deeper taxonomy applied at the clause level, organized into modules. The first module, `tax`, covers tax provisions; its nodes use dotted identifiers (`tax.1`, `tax.1.1`, `tax.1.1.1`). `tax_clause_assignments` links clauses to these nodes.

### `tax_clause_taxonomy_l1`

Top level of the tax-module clause taxonomy (e.g. tax.1 "Intended Tax Treatment, Elections, and Purchase Price Allocation").

**Primary key**: `standard_id`

**Relationships**:
- `tax_clause_taxonomy_l2.parent_id` → `tax_clause_taxonomy_l1.standard_id` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `standard_id` | `varchar(64)` | no | Primary key; dotted identifier (`tax.N`). |
| `label` | `text` | yes | Human-readable category name. |

### `tax_clause_taxonomy_l2`

Middle level of the tax-module clause taxonomy.

**Primary key**: `standard_id`

**Relationships**:
- `tax_clause_taxonomy_l2.parent_id` → `tax_clause_taxonomy_l1.standard_id` (many-to-one)
- `tax_clause_taxonomy_l3.parent_id` → `tax_clause_taxonomy_l2.standard_id` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `standard_id` | `varchar(64)` | no | Primary key; dotted identifier (`tax.N.N`). |
| `label` | `text` | yes | Human-readable category name. |
| `parent_id` | `varchar(64)` | yes | The `tax_clause_taxonomy_l1.standard_id` this node belongs to. |

**Indexes**: `idx_tax_clause_taxonomy_l2_parent` (`parent_id`)

### `tax_clause_taxonomy_l3`

Leaf level of the tax-module clause taxonomy (e.g. tax.1.1.1 "Intended Tax-Free Reorganization").

**Primary key**: `standard_id`

**Relationships**:
- `tax_clause_taxonomy_l3.parent_id` → `tax_clause_taxonomy_l2.standard_id` (many-to-one)
- `tax_clause_assignments.standard_id` → `tax_clause_taxonomy_l3.standard_id` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `standard_id` | `varchar(64)` | no | Primary key; dotted identifier (`tax.N.N.N`). |
| `label` | `text` | yes | Human-readable category name. |
| `parent_id` | `varchar(64)` | yes | The `tax_clause_taxonomy_l2.standard_id` this node belongs to. |

**Indexes**: `idx_tax_clause_taxonomy_l3_parent` (`parent_id`)

### `tax_clause_assignments`

Assignments of clauses to clause-taxonomy nodes. `standard_id` may point at an L2 or an L3 node — clauses are labeled at the most specific level the model supports for that branch.

**Primary key**: `clause_uuid`, `standard_id`

**Relationships**:
- `tax_clause_assignments.clause_uuid` → `clauses.clause_uuid` (many-to-one)
- `tax_clause_assignments.standard_id` → `tax_clause_taxonomy_l3.standard_id` (many-to-one)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `clause_uuid` | `char(36)` | no | The `clauses.clause_uuid` being labeled (composite PK with `standard_id`). |
| `standard_id` | `varchar(64)` | no | The assigned taxonomy node — a `tax_clause_taxonomy_l2` or `tax_clause_taxonomy_l3` `standard_id` (composite PK with `clause_uuid`). |
| `is_gold_label` | `tinyint(1)` | no | 1 if the assignment is a curated gold label rather than a raw model output. |
| `model_name` | `varchar(128)` | yes | Model that produced the assignment, when model-generated. |
| `assigned_at` | `datetime` | yes | When the assignment was made (UTC). |

**Indexes**: `idx_tax_clause_assignments_standard_clause` (`standard_id`, `clause_uuid`); `idx_tax_clause_assignments_standard_id` (`standard_id`)

## Search denormalizations

Precomputed tables that power section search in the app and API. They duplicate agreement metadata per section so searches can filter without joins; if you are doing your own analysis, prefer joining the source tables directly.

### `latest_sections_search`

Denormalized search index: one row per section of the latest verified XML of each public agreement, with the deal metadata from `agreements` copied onto every row so searches can filter without joins. Metadata columns have the same meaning as their `agreements` counterparts.

**Primary key**: `section_uuid`

**Relationships**:
- `latest_sections_search.section_uuid` ↔ `sections.section_uuid` (one-to-one)
- `latest_sections_search.agreement_uuid` → `agreements.agreement_uuid` (many-to-one)
- `latest_sections_search_standard_ids.section_uuid` → `latest_sections_search.section_uuid` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `section_uuid` | `char(36)` | no | Primary key; the `sections.section_uuid` this row mirrors. |
| `agreement_uuid` | `char(36)` | no | Agreement the section belongs to. |
| `filing_date` | `date` | yes | Copied from `agreements.filing_date`. |
| `prob_filing` | `float` | yes | Copied from `agreements.prob_filing`. |
| `filing_company_name` | `varchar(1024)` | yes | Copied from `agreements.filing_company_name`. |
| `filing_company_cik` | `varchar(256)` | yes | Copied from `agreements.filing_company_cik`. |
| `form_type` | `varchar(256)` | yes | Copied from `agreements.form_type`. |
| `exhibit_type` | `enum('10','2','99')` | yes | Copied from `agreements.exhibit_type`. |
| `target` | `varchar(512)` | yes | Copied from `agreements.target`. |
| `acquirer` | `varchar(512)` | yes | Copied from `agreements.acquirer`. |
| `transaction_price_total` | `decimal(18,2)` | yes | Copied from `agreements.transaction_price_total`. |
| `transaction_price_stock` | `decimal(18,2)` | yes | Copied from `agreements.transaction_price_stock`. |
| `transaction_price_cash` | `decimal(18,2)` | yes | Copied from `agreements.transaction_price_cash`. |
| `transaction_price_assets` | `decimal(18,2)` | yes | Copied from `agreements.transaction_price_assets`. |
| `transaction_consideration` | `enum('stock','cash','assets','mixed','unknown')` | yes | Copied from `agreements.transaction_consideration`. |
| `target_type` | `enum('public','private')` | yes | Copied from `agreements.target_type`. |
| `acquirer_type` | `enum('public','private')` | yes | Copied from `agreements.acquirer_type`. |
| `target_counsel` | `longtext` | yes | Copied from `agreements.target_counsel`. |
| `acquirer_counsel` | `longtext` | yes | Copied from `agreements.acquirer_counsel`. |
| `target_industry` | `varchar(128)` | yes | Copied from `agreements.target_industry`. |
| `acquirer_industry` | `varchar(128)` | yes | Copied from `agreements.acquirer_industry`. |
| `announce_date` | `date` | yes | Copied from `agreements.announce_date`. |
| `close_date` | `date` | yes | Copied from `agreements.close_date`. |
| `deal_status` | `enum('pending','complete','cancelled')` | yes | Copied from `agreements.deal_status`. |
| `attitude` | `enum('friendly','hostile','unsolicited')` | yes | Copied from `agreements.attitude`. |
| `deal_type` | `enum('merger','stock_acquisition','asset_acquisition','tender_offer','membership_interest_purchase')` | yes | Copied from `agreements.deal_type`. |
| `purpose` | `enum('strategic','financial')` | yes | Copied from `agreements.purpose`. |
| `target_pe` | `tinyint(1)` | yes | Copied from `agreements.target_pe`. |
| `acquirer_pe` | `tinyint(1)` | yes | Copied from `agreements.acquirer_pe`. |
| `verified` | `tinyint(1)` | yes | Copied from `agreements.verified`. |
| `url` | `varchar(512)` | yes | Copied from `agreements.url`. |
| `section_standard_ids` | `varchar(512)` | yes | JSON array of section-taxonomy `standard_id`s; mirrors `sections.section_standard_id_gold_label`. |
| `article_title` | `mediumtext` | yes | Copied from `sections.article_title`. |
| `section_title` | `mediumtext` | yes | Copied from `sections.section_title`. |

**Indexes**: `idx_lss_acquirer_industry_filing_section` (`acquirer_industry`, `filing_date`, `section_uuid`); `idx_lss_acquirer_pe` (`acquirer_pe`); `idx_lss_acquirer_section` (`acquirer`, `section_uuid`); `idx_lss_acquirer_type` (`acquirer_type`); `idx_lss_agreement` (`agreement_uuid`); `idx_lss_attitude` (`attitude`); `idx_lss_deal_status` (`deal_status`); `idx_lss_deal_type_filing_section` (`deal_type`, `filing_date`, `section_uuid`); `idx_lss_filing_date_section` (`filing_date`, `section_uuid`); `idx_lss_purpose` (`purpose`); `idx_lss_section_filing` (`section_uuid`, `filing_date`); `idx_lss_standard_id` (`section_standard_ids`); `idx_lss_target_industry_filing_section` (`target_industry`, `filing_date`, `section_uuid`); `idx_lss_target_pe` (`target_pe`); `idx_lss_target_section` (`target`, `section_uuid`); `idx_lss_target_type` (`target_type`); `idx_lss_transaction_consideration` (`transaction_consideration`); `idx_lss_txn_price_assets` (`transaction_price_assets`); `idx_lss_txn_price_cash` (`transaction_price_cash`); `idx_lss_txn_price_stock` (`transaction_price_stock`); `idx_lss_txn_price_total` (`transaction_price_total`)

### `latest_sections_search_standard_ids`

Exploded one-row-per-(taxonomy node, section) form of `latest_sections_search.section_standard_ids`, for indexed taxonomy filtering.

**Primary key**: `standard_id`, `section_uuid`

**Relationships**:
- `latest_sections_search_standard_ids.section_uuid` → `latest_sections_search.section_uuid` (many-to-one)
- `latest_sections_search_standard_ids.standard_id` → `taxonomy_l3.standard_id` (many-to-one)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `standard_id` | `varchar(255)` | no | A section-taxonomy node (`taxonomy_l2` or `taxonomy_l3` `standard_id`) assigned to the section (composite PK with `section_uuid`). |
| `section_uuid` | `char(36)` | no | The labeled section (composite PK with `standard_id`). |
| `agreement_uuid` | `char(36)` | no | Agreement the section belongs to. |

**Indexes**: `idx_lsssi_agreement_uuid` (`agreement_uuid`); `idx_lsssi_section_uuid_standard_id` (`section_uuid`, `standard_id`); `idx_lsssi_standard_id_agreement_uuid` (`standard_id`, `agreement_uuid`)

## Counsel

Law firms advising each side of each deal, extracted from the agreements and normalized to canonical firm names.

### `counsel`

Canonical law firms, deduplicated by normalized name.

**Primary key**: `counsel_id`

**Relationships**:
- `agreement_counsel.counsel_id` → `counsel.counsel_id` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `counsel_id` | `bigint(20) unsigned` | no | Primary key (auto-increment). |
| `canonical_name` | `varchar(512)` | no | Canonical display name of the firm. |
| `canonical_name_normalized` | `varchar(512)` | no | Normalized name used for deduplication; unique. |
| `created_at` | `datetime` | no | Row creation time (UTC). |
| `updated_at` | `datetime` | no | Row last-update time (UTC). |

**Indexes**: `idx_counsel_canonical_name` (`canonical_name`); `uq_counsel_canonical_name_normalized` (unique, `canonical_name_normalized`)

### `agreement_counsel`

Law firms advising each side of each agreement, in the order they were listed. Join through `counsel` for canonical firm names.

**Primary key**: `agreement_uuid`, `side`, `position`

**Relationships**:
- `agreement_counsel.agreement_uuid` → `agreements.agreement_uuid` (many-to-one)
- `agreement_counsel.counsel_id` → `counsel.counsel_id` (many-to-one)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `agreement_uuid` | `char(36)` | no | The agreement (composite PK with `side` and `position`). |
| `side` | `enum('target','acquirer')` | no | Which party the firm advised (target or acquirer). |
| `position` | `int(11)` | no | Order of the firm within that side's counsel list. |
| `raw_name` | `varchar(1024)` | no | Firm name as extracted from the agreement, before canonicalization. |
| `counsel_id` | `bigint(20) unsigned` | no | The canonical firm (`counsel.counsel_id`). |
| `created_at` | `datetime` | no | Row creation time (UTC). |
| `updated_at` | `datetime` | no | Row last-update time (UTC). |

**Indexes**: `idx_agreement_counsel_counsel_id` (`counsel_id`); `idx_agreement_counsel_side_counsel_id` (`side`, `counsel_id`)

## Reference data

NAICS industry classification reference tables.

### `naics_sectors`

NAICS sector reference (two-digit sector codes) with Pandects' groupings.

**Primary key**: `sector_code`

**Relationships**:
- `naics_sub_sectors.sector_code` → `naics_sectors.sector_code` (one-to-many)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `super_sector` | `text` | yes | Broadest Pandects grouping. |
| `sector_group` | `text` | yes | Pandects grouping of related sectors. |
| `sector_desc` | `text` | yes | Official NAICS sector description. |
| `sector_code` | `bigint(20)` | no | Primary key; two-digit NAICS sector code. |

### `naics_sub_sectors`

NAICS subsector reference (three-digit codes).

**Primary key**: `sub_sector_code`

**Relationships**:
- `naics_sub_sectors.sector_code` → `naics_sectors.sector_code` (many-to-one)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `sub_sector_desc` | `text` | yes | Official NAICS subsector description. |
| `sub_sector_code` | `bigint(20)` | no | Primary key; three-digit NAICS subsector code. |
| `sector_code` | `bigint(20)` | no | Parent NAICS sector (`naics_sectors.sector_code`). |

**Indexes**: `idx_naics_sub_sectors_sector_code` (`sector_code`)

## Analytics summaries

Pre-aggregated statistics that power the public data/analytics pages. Everything here is derivable from the core tables; these exist so the app can serve charts without heavy queries.

### `summary_data`

Pre-aggregated corpus statistics: one row per observed combination of filing year, form type, exhibit type, party types, deal type, attitude, and purpose, with agreement/page/section counts and total deal value.

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `year` | `int(11)` | yes | Filing year of the aggregated agreements. |
| `form_type` | `varchar(1025)` | yes | SEC form type dimension. |
| `exhibit_type` | `enum('10','2','99')` | yes | Exhibit series dimension. |
| `target_type` | `enum('public','private')` | yes | Target ownership dimension (public/private). |
| `acquirer_type` | `enum('public','private')` | yes | Acquirer ownership dimension (public/private). |
| `deal_type` | `enum('merger','stock_acquisition','asset_acquisition','tender_offer','membership_interest_purchase')` | yes | Transaction structure dimension. |
| `attitude` | `enum('friendly','hostile','unsolicited')` | yes | Deal attitude dimension. |
| `purpose` | `enum('strategic','financial')` | yes | Deal purpose dimension. |
| `count_agreements` | `bigint(20)` | no | Number of agreements in this bucket. |
| `count_pages` | `bigint(20)` | no | Number of source-document pages across those agreements. |
| `count_sections` | `bigint(20)` | no | Number of sections across those agreements. |
| `count_distinct_acquirer` | `bigint(20)` | no | Distinct acquirer names in this bucket. |
| `count_distinct_target` | `bigint(20)` | no | Distinct target names in this bucket. |
| `sum_transaction_value_total` | `decimal(18,2)` | no | Sum of total transaction values, in USD. |
| `count_verified` | `bigint(20)` | no | Number of verified agreements in this bucket. |

### `agreement_overview_summary`

Single-row headline statistics for the data overview page.

**Primary key**: `singleton_key`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `singleton_key` | `tinyint(4)` | no | Always 1; enforces the single row. |
| `metadata_coverage_pct` | `decimal(5,1)` | yes | Share of eligible agreements with extracted deal metadata. |
| `taxonomy_coverage_pct` | `decimal(5,1)` | yes | Share of eligible sections with taxonomy labels. |
| `latest_filing_date` | `date` | yes | Most recent `agreements.filing_date` in the corpus. |
| `metadata_covered_agreements` | `bigint(20)` | yes | Number of agreements with extracted deal metadata. |
| `taxonomy_covered_sections` | `bigint(20)` | yes | Number of sections with taxonomy labels. |

### `agreement_index_summary`

One row per public agreement, powering the agreement browse/index pages.

**Primary key**: `agreement_uuid`

**Relationships**:
- `agreement_index_summary.agreement_uuid` ↔ `agreements.agreement_uuid` (one-to-one)

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `agreement_uuid` | `char(36)` | no | Primary key; the agreement. |
| `year` | `int(11)` | yes | Filing year. |
| `target` | `varchar(1024)` | yes | Target company name. |
| `acquirer` | `varchar(1024)` | yes | Acquirer company name. |
| `verified` | `tinyint(1)` | no | 1 if the agreement is verified. |

**Indexes**: `idx_agreement_index_acquirer` (`acquirer`); `idx_agreement_index_target` (`target`); `idx_agreement_index_year` (`year`, `agreement_uuid`)

### `agreement_filter_option_summary`

Distinct filter options offered by the search UI, with the number of agreements each matches.

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `field_name` | `varchar(64)` | no | Which filter the option belongs to: targets, acquirers, target_industries, acquirer_industries, target_counsels, or acquirer_counsels. |
| `option_value` | `varchar(1024)` | no | The filterable value. |
| `agreement_count` | `bigint(20)` | no | Number of agreements matching this option. |

**Indexes**: `idx_filter_option_lookup` (`field_name`, `option_value`)

### `agreement_metadata_field_coverage_summary`

Extraction coverage per deal-metadata field: how many agreements are eligible for the field and how many have it populated, over all ingested agreements and over fully processed agreements.

**Primary key**: `field_name`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `field_name` | `varchar(64)` | no | Primary key; the `agreements` metadata column being measured. |
| `label` | `varchar(128)` | no | Display label for the field. |
| `ingested_eligible_agreements` | `bigint(20)` | no | Ingested agreements eligible for this field. |
| `ingested_covered_agreements` | `bigint(20)` | no | Ingested agreements with the field populated. |
| `ingested_coverage_pct` | `decimal(5,1)` | yes | Coverage percentage over ingested agreements. |
| `processed_eligible_agreements` | `bigint(20)` | no | Fully processed agreements eligible for this field. |
| `processed_covered_agreements` | `bigint(20)` | no | Fully processed agreements with the field populated. |
| `processed_coverage_pct` | `decimal(5,1)` | yes | Coverage percentage over fully processed agreements. |
| `note` | `text` | no | Caveats about how coverage is measured for this field. |

### `agreement_status_summary`

Pipeline progress counts by filing year: how many agreements sit at each processing stage.

**Primary key**: `year`, `color`, `current_stage`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `year` | `int(11)` | no | Filing year. |
| `color` | `varchar(16)` | no | Display bucket for the stage (gray, green, or red). |
| `current_stage` | `varchar(32)` | no | Pipeline stage (e.g. 1_pre_processing, 3_xml, processed). |
| `count` | `int(11)` | no | Number of agreements at this stage for this year. |

### `agreement_deal_type_summary`

Deal counts by filing year and transaction structure.

**Primary key**: `year`, `deal_type`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `year` | `int(11)` | no | Filing year. |
| `deal_type` | `varchar(64)` | no | Transaction structure. |
| `count` | `bigint(20)` | no | Number of agreements. |

### `agreement_target_industry_summary`

Deal count and total transaction value by filing year and target industry.

**Primary key**: `year`, `industry`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `year` | `int(11)` | no | Filing year. |
| `industry` | `varchar(255)` | no | Target industry label. |
| `deal_count` | `bigint(20)` | no | Number of agreements. |
| `total_transaction_value` | `decimal(24,2)` | no | Sum of total transaction values, in USD. |

### `agreement_industry_pairing_summary`

Deal count and total transaction value for each (target industry, acquirer industry) pairing.

**Primary key**: `target_industry`, `acquirer_industry`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `target_industry` | `varchar(255)` | no | Target industry label. |
| `acquirer_industry` | `varchar(255)` | no | Acquirer industry label. |
| `deal_count` | `bigint(20)` | no | Number of agreements. |
| `total_transaction_value` | `decimal(24,2)` | no | Sum of total transaction values, in USD. |

### `agreement_ownership_mix_summary`

Deal count and total transaction value by filing year and target ownership bucket.

**Primary key**: `year`, `target_bucket`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `year` | `int(11)` | no | Filing year. |
| `target_bucket` | `varchar(32)` | no | Target ownership bucket (public or private). |
| `deal_count` | `bigint(20)` | no | Number of agreements. |
| `total_transaction_value` | `decimal(24,2)` | no | Sum of total transaction values, in USD. |

### `agreement_ownership_deal_size_summary`

Transaction-value quartiles by filing year and target ownership bucket.

**Primary key**: `year`, `target_bucket`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `year` | `int(11)` | no | Filing year. |
| `target_bucket` | `varchar(32)` | no | Target ownership bucket (public or private). |
| `deal_count` | `bigint(20)` | no | Number of agreements. |
| `p25_transaction_value` | `decimal(24,2)` | yes | 25th-percentile total transaction value, in USD. |
| `median_transaction_value` | `decimal(24,2)` | yes | Median total transaction value, in USD. |
| `p75_transaction_value` | `decimal(24,2)` | yes | 75th-percentile total transaction value, in USD. |

### `agreement_buyer_type_matrix_summary`

Deal count and median transaction value for each target ownership bucket x buyer type cell.

**Primary key**: `target_bucket`, `buyer_bucket`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `target_bucket` | `varchar(32)` | no | Target ownership bucket (public or private). |
| `buyer_bucket` | `varchar(32)` | no | Buyer type: private_equity, private_strategic, public_buyer, or other. |
| `deal_count` | `bigint(20)` | no | Number of agreements. |
| `median_transaction_value` | `decimal(24,2)` | yes | Median total transaction value, in USD. |

### `agreement_counsel_leaderboard_summary`

Counsel league table: deal count and total transaction value per law firm, by side and filing year.

**Primary key**: `side`, `counsel_key`, `year`

| Column | Type | Nullable | Description |
| --- | --- | --- | --- |
| `side` | `varchar(16)` | no | buy_side or sell_side. |
| `counsel_key` | `varchar(255)` | no | Normalized firm key (composite PK with `side` and `year`). |
| `counsel` | `varchar(255)` | no | Display name of the firm. |
| `year` | `int(11)` | no | Filing year. |
| `deal_count` | `bigint(20)` | no | Number of deals the firm advised on. |
| `total_transaction_value` | `decimal(24,2)` | no | Sum of total transaction values, in USD. |

**Indexes**: `idx_counsel_leaderboard_side_year` (`side`, `year`)
