---
alwaysApply: false
---
<project_background>
Repo background:
- `frontend/` contains the Vite SPA, SSR/prerender server code, and shared frontend types.
- `backend/` contains the Flask API, auth/session logic, and API-facing schemas.
- `etl/` contains the Dagster pipeline plus ML/data-processing code used to ingest and transform agreements.
- Generated artifacts, large backfill datasets, and notebooks exist in the repo, but they are not normal editing targets unless the task is explicitly about them.
</project_background>
