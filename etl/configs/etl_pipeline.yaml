resources:
  pipeline_config:
    config:
      mode: from_scratch
      scope: batched
      tagging_agreement_batch_size: 500
      xml_agreement_batch_size: 10
      taxonomy_agreement_batch_size: 50
      ai_repair_agreement_batch_size: 150
      tx_metadata_agreement_batch_size: 10
